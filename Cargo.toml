[package]
name = "determa-state"
version = "0.0.6"
edition = "2021"
authors = ["fruwehq"]
license = "MIT"
description = "Determa State — Rust implementation of the Determa statechart engine (implements Determa State spec v0.0.6)"
repository = "https://github.com/fruwehq/determa-state-rust"
readme = "README.md"
exclude = [
    "authoritative-spec/",
    "conformance-suite/",
    "tests/core_conformance.rs",
    ".github/",
]

[lib]
name = "determa_state"
path = "src/lib.rs"

[[bin]]
name = "determa-state"
path = "src/bin/determa_state.rs"

# A language-suffixed alias so the launcher (and users) can pick a specific
# implementation when both are installed (DETERMA_STATE_IMPL=rust). Same binary.
[[bin]]
name = "determa-state-rust"
path = "src/bin/determa_state_rust.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
cel-interpreter = "0.10"
jsonschema = { version = "0.33", default-features = false }
regex = "1"
sha2 = "0.10"

[profile.release]
opt-level = 2
